/**
 * agent-definition — IS marketplace-tier authoring contract #3 (subagent frontmatter — DR-062 projection-mirrored v2 base).
 *
 * GENERATED by scripts/codegen-authoring.ts from the three-artifact composition:
 *   schemas/authoring/v2/upstream-base/agent-definition.v2.json   (authored by THEM)
 *   schemas/authoring/v2/marketplace-tier.schema.json#/$defs/universalFolds
 *   schemas/authoring/v2/is-overlay/agent-definition.v2.json      (authored by US)
 *   ⇒ schemas/authoring/v2/agent-definition.schema.json           (pure allOf)
 *
 * DO NOT EDIT BY HAND — re-run `pnpm run codegen:authoring` after a schema edit.
 * Per DR-044 D8 the Zod validator AND the D7 inline `$comment` effective-required
 * manifest are single-sourced from the JSON Schema: the two layers below are
 * derived field-by-field from the base and overlay `required` arrays + the
 * per-field type/constraint surface; the universal folds are reused by reference
 * from marketplace-tier. The monotonic-additive invariant (the overlay only ADDS
 * required fields and NARROWS constraints on the base) is the 2026-04-28-debacle
 * guard, asserted by the property test in
 * src/__tests__/agent-definition-v2-schema.test.ts.
 */

import {
  type AuthoringArtifact,
  type FoldIssue,
  attach,
  requiredFieldsIssues,
  universalFoldsIssues,
} from './marketplace-tier.js';

// ─── Required-field sets (the source of the effective-required manifest) ─────

/** standardFloor — the upstream-base always-required fields (code.claude.com sub-agents). */
export const AGENT_DEFINITION_BASE_REQUIRED = ['name', 'description'] as const;

/** The IS-overlay required delta (beyond the base floor). */
export const AGENT_DEFINITION_OVERLAY_REQUIRED = [
  'tools',
  'model',
  'color',
  'version',
  'author',
  'tags',
] as const;

/** Effective required = base ∪ overlay = the IS 8-field marketplace set (NON-NEGOTIABLE). */
export const AGENT_DEFINITION_REQUIRED_FIELDS = [
  ...AGENT_DEFINITION_BASE_REQUIRED,
  ...AGENT_DEFINITION_OVERLAY_REQUIRED,
] as const;

// ─── Constraint constants (mirror the JSON Schemas exactly) ──────────────────

/** code.claude.com sub-agents permissionMode allowed values (upstream-base). */
export const AGENT_PERMISSIONMODE_VALUES = [
  'default',
  'acceptEdits',
  'auto',
  'dontAsk',
  'bypassPermissions',
  'plan',
] as const;
/** code.claude.com sub-agents maxTurns numeric floor (upstream-base). */
export const AGENT_MAXTURNS_MIN = 0;
/** code.claude.com sub-agents memory allowed values (upstream-base). */
export const AGENT_MEMORY_VALUES = ['user', 'project', 'local'] as const;
/** code.claude.com sub-agents effort allowed values (upstream-base). */
export const AGENT_EFFORT_VALUES = ['low', 'medium', 'high', 'xhigh', 'max'] as const;
/** code.claude.com sub-agents isolation allowed values (upstream-base). */
export const AGENT_ISOLATION_VALUES = ['worktree'] as const;
/** code.claude.com sub-agents color allowed values (upstream-base). */
export const AGENT_COLOR_VALUES = [
  'red',
  'blue',
  'green',
  'yellow',
  'purple',
  'orange',
  'pink',
  'cyan',
] as const;
/** IS model allowed values (is-overlay — DR-062 relocated closed-enum narrowing). */
export const AGENT_MODEL_VALUES = ['sonnet', 'opus', 'haiku', 'fable', 'inherit'] as const;
/** IS kebab-case name surface (is-overlay — DR-062 relocated structural encoding). */
export const AGENT_NAME_PATTERN = /^[a-z0-9]([a-z0-9-]*[a-z0-9])?$/;
/** IS name length ceiling (is-overlay — DR-062 relocated structural encoding). */
export const AGENT_NAME_MAX = 64;
/** Strict SemVer 2.0.0 (is-overlay — stricter than the legacy IS prefix match). */
export const SEMVER_PATTERN =
  /^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/;

function isStringArray(value: unknown): value is string[] {
  return Array.isArray(value) && value.every((item) => typeof item === 'string');
}

function isPlainObject(value: unknown): value is Record<string, unknown> {
  return typeof value === 'object' && value !== null && !Array.isArray(value);
}

// ─── Layer 1: upstream base (authored by THEM) ───────────────────────────────

/**
 * The DR-062 projection-mirrored v2 base (REGENERATED from the captured
 * projection, spec_version unversioned-2026-06-12 — not a byte-copy of the
 * frozen v1 base). Required presence of upstream's standardFloor
 * ([name, description] — 'Only name and description are required') +
 * type/constraint on the FULL documented surface: name as a bare string
 * (kebab-case is prose-only upstream — the structural encoding lives in
 * the overlay per DR-062 C3, 059 #5); tools as the documented
 * comma-separated-string|array union (059 #3 C2); model as a bare string
 * (the documented full-model-ID latitude — 059 #4 C2); the documented
 * enums permissionMode/memory/effort/isolation/color (magenta NOT
 * adopted — 059 #6 C4); maxTurns as a non-negative integer; skills as an
 * array of strings; mcpServers as a bare array (string|object entries —
 * no single documented item shape); hooks as a generic object;
 * disallowedTools/initialPrompt as strings. Length of `description` is
 * intentionally NOT capped here — the universal disclosureMarkers fold
 * (v2: 1024) is the operative cap.
 */
export function upstreamBaseIssues(artifact: AuthoringArtifact): FoldIssue[] {
  const issues: FoldIssue[] = [...requiredFieldsIssues(artifact, AGENT_DEFINITION_BASE_REQUIRED)];

  if ('name' in artifact && typeof artifact['name'] !== 'string') {
    issues.push({ message: 'name must be a string', path: ['name'] });
  }

  if ('description' in artifact) {
    const description = artifact['description'];
    if (typeof description !== 'string') {
      issues.push({ message: 'description must be a string', path: ['description'] });
    } else if (description.length < 1) {
      issues.push({ message: 'description must not be empty', path: ['description'] });
    }
  }

  if ('tools' in artifact) {
    const tools = artifact['tools'];
    if (typeof tools !== 'string' && !isStringArray(tools)) {
      issues.push({
        message: 'tools must be a string or an array of strings',
        path: ['tools'],
      });
    }
  }

  if ('disallowedTools' in artifact && typeof artifact['disallowedTools'] !== 'string') {
    issues.push({ message: 'disallowedTools must be a string', path: ['disallowedTools'] });
  }

  if ('model' in artifact && typeof artifact['model'] !== 'string') {
    issues.push({ message: 'model must be a string', path: ['model'] });
  }

  if ('permissionMode' in artifact) {
    const permissionMode = artifact['permissionMode'];
    if (typeof permissionMode !== 'string') {
      issues.push({ message: 'permissionMode must be a string', path: ['permissionMode'] });
    } else if (!(AGENT_PERMISSIONMODE_VALUES as readonly string[]).includes(permissionMode)) {
      issues.push({
        message: `permissionMode must be one of: ${AGENT_PERMISSIONMODE_VALUES.join(', ')}`,
        path: ['permissionMode'],
      });
    }
  }

  if ('maxTurns' in artifact) {
    const maxTurns = artifact['maxTurns'];
    if (typeof maxTurns !== 'number' || !Number.isInteger(maxTurns)) {
      issues.push({ message: 'maxTurns must be an integer', path: ['maxTurns'] });
    } else if (maxTurns < AGENT_MAXTURNS_MIN) {
      issues.push({
        message: `maxTurns must be at least ${AGENT_MAXTURNS_MIN}`,
        path: ['maxTurns'],
      });
    }
  }

  if ('skills' in artifact && !isStringArray(artifact['skills'])) {
    issues.push({ message: 'skills must be an array of strings', path: ['skills'] });
  }

  if ('mcpServers' in artifact && !Array.isArray(artifact['mcpServers'])) {
    issues.push({ message: 'mcpServers must be an array', path: ['mcpServers'] });
  }

  if ('hooks' in artifact && !isPlainObject(artifact['hooks'])) {
    issues.push({ message: 'hooks must be an object', path: ['hooks'] });
  }

  if ('memory' in artifact) {
    const memory = artifact['memory'];
    if (typeof memory !== 'string') {
      issues.push({ message: 'memory must be a string', path: ['memory'] });
    } else if (!(AGENT_MEMORY_VALUES as readonly string[]).includes(memory)) {
      issues.push({
        message: `memory must be one of: ${AGENT_MEMORY_VALUES.join(', ')}`,
        path: ['memory'],
      });
    }
  }

  if ('background' in artifact && typeof artifact['background'] !== 'boolean') {
    issues.push({ message: 'background must be a boolean', path: ['background'] });
  }

  if ('effort' in artifact) {
    const effort = artifact['effort'];
    if (typeof effort !== 'string') {
      issues.push({ message: 'effort must be a string', path: ['effort'] });
    } else if (!(AGENT_EFFORT_VALUES as readonly string[]).includes(effort)) {
      issues.push({
        message: `effort must be one of: ${AGENT_EFFORT_VALUES.join(', ')}`,
        path: ['effort'],
      });
    }
  }

  if ('isolation' in artifact) {
    const isolation = artifact['isolation'];
    if (typeof isolation !== 'string') {
      issues.push({ message: 'isolation must be a string', path: ['isolation'] });
    } else if (!(AGENT_ISOLATION_VALUES as readonly string[]).includes(isolation)) {
      issues.push({
        message: `isolation must be one of: ${AGENT_ISOLATION_VALUES.join(', ')}`,
        path: ['isolation'],
      });
    }
  }

  if ('color' in artifact) {
    const color = artifact['color'];
    if (typeof color !== 'string') {
      issues.push({ message: 'color must be a string', path: ['color'] });
    } else if (!(AGENT_COLOR_VALUES as readonly string[]).includes(color)) {
      issues.push({
        message: `color must be one of: ${AGENT_COLOR_VALUES.join(', ')}`,
        path: ['color'],
      });
    }
  }

  if ('initialPrompt' in artifact && typeof artifact['initialPrompt'] !== 'string') {
    issues.push({ message: 'initialPrompt must be a string', path: ['initialPrompt'] });
  }

  return issues;
}

// ─── Layer 3: IS overlay (authored by US) ────────────────────────────────────

/**
 * The IS-only v2 delta: three upstream-optional fields promoted to
 * IS-required (tools, model, color) + three net-new IS-required tracking
 * fields (version, author, tags — the same trio skill-frontmatter
 * carries) + the SemVer narrowing on version + the RELOCATED tools
 * array-only narrowing (DR-062 C2, 059 #3 — the v2 base carries upstream's
 * string|array union) + the RELOCATED model closed-enum narrowing
 * (DR-062 C2, 059 #4 — the v2 base carries the documented full-model-ID
 * latitude) + the RELOCATED structural name encoding (kebab pattern +
 * 64-char cap; DR-062 C3, 059 #5 — prose-only upstream) + the RELOCATED
 * optional `metadata` extension object (DR-062 C3, 059 #2). A field whose
 * type is already enforced by the base is not re-typed here — its
 * presence is the required check — to keep messages single-sourced.
 */
export function isOverlayIssues(artifact: AuthoringArtifact): FoldIssue[] {
  const issues: FoldIssue[] = [
    ...requiredFieldsIssues(artifact, AGENT_DEFINITION_OVERLAY_REQUIRED),
  ];

  if ('tools' in artifact && !isStringArray(artifact['tools'])) {
    issues.push({ message: 'tools must be an array of strings', path: ['tools'] });
  }

  if ('model' in artifact) {
    const model = artifact['model'];
    if (typeof model !== 'string') {
      issues.push({ message: 'model must be a string', path: ['model'] });
    } else if (!(AGENT_MODEL_VALUES as readonly string[]).includes(model)) {
      issues.push({
        message: `model must be one of: ${AGENT_MODEL_VALUES.join(', ')}`,
        path: ['model'],
      });
    }
  }

  if ('version' in artifact) {
    const version = artifact['version'];
    if (typeof version !== 'string') {
      issues.push({ message: 'version must be a string', path: ['version'] });
    } else if (!SEMVER_PATTERN.test(version)) {
      issues.push({ message: 'version must be strict SemVer 2.0.0', path: ['version'] });
    }
  }

  if ('author' in artifact) {
    const author = artifact['author'];
    if (typeof author !== 'string') {
      issues.push({ message: 'author must be a string', path: ['author'] });
    } else if (author.length < 1) {
      issues.push({ message: 'author must not be empty', path: ['author'] });
    }
  }

  if ('tags' in artifact && !isStringArray(artifact['tags'])) {
    issues.push({ message: 'tags must be an array of strings', path: ['tags'] });
  }

  if ('name' in artifact) {
    const name = artifact['name'];
    if (typeof name !== 'string') {
      issues.push({ message: 'name must be a string', path: ['name'] });
    } else {
      if (name.length > AGENT_NAME_MAX) {
        issues.push({
          message: `name must be at most ${AGENT_NAME_MAX} characters`,
          path: ['name'],
        });
      }
      if (!AGENT_NAME_PATTERN.test(name)) {
        issues.push({
          message: 'name must be kebab-case (lowercase letters, digits, hyphens)',
          path: ['name'],
        });
      }
    }
  }

  if ('metadata' in artifact && !isPlainObject(artifact['metadata'])) {
    issues.push({ message: 'metadata must be an object', path: ['metadata'] });
  }

  return issues;
}

// ─── The composition (allOf of base + universal folds + overlay) ─────────────

/** Every issue from the three composed layers, in layer order. */
export function agentDefinitionIssues(artifact: AuthoringArtifact): FoldIssue[] {
  return [
    ...upstreamBaseIssues(artifact),
    ...universalFoldsIssues(artifact),
    ...isOverlayIssues(artifact),
  ];
}

/** The agent-definition contract — pure allOf composition (base ∧ universalFolds ∧ overlay). */
export const AgentDefinitionSchema = attach(agentDefinitionIssues);

export type AgentDefinition = AuthoringArtifact;
