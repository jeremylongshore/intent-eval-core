/**
 * marketplace-catalog — IS marketplace-tier authoring contract #6 (marketplace.json catalog — DR-062 projection-mirrored v2 base).
 *
 * GENERATED by scripts/codegen-authoring.ts from the three-artifact composition:
 *   schemas/authoring/v2/upstream-base/marketplace-catalog.v2.json   (authored by THEM)
 *   schemas/authoring/v2/marketplace-tier.schema.json#/$defs/universalFolds
 *   schemas/authoring/v2/is-overlay/marketplace-catalog.v2.json      (authored by US)
 *   ⇒ schemas/authoring/v2/marketplace-catalog.schema.json           (pure allOf)
 *
 * DO NOT EDIT BY HAND — re-run `pnpm run codegen:authoring` after a schema edit.
 * Per DR-044 D8 the Zod validator AND the D7 inline `$comment` effective-required
 * manifest are single-sourced from the JSON Schema: the two layers below are
 * derived field-by-field from the base and overlay `required` arrays + the
 * per-field type/constraint surface; the universal folds are reused by reference
 * from marketplace-tier. The monotonic-additive invariant (the overlay only ADDS
 * required fields and NARROWS constraints on the base) is the 2026-04-28-debacle
 * guard, asserted by the property test in
 * src/__tests__/marketplace-catalog-v2-schema.test.ts.
 */

import {
  type AuthoringArtifact,
  type FoldIssue,
  attach,
  requiredFieldsIssues,
  universalFoldsIssues,
} from './marketplace-tier.js';

// ─── Required-field sets (the source of the effective-required manifest) ─────

/** standardFloor — the upstream-base always-required fields (code.claude.com plugin-marketplaces). */
export const MARKETPLACE_CATALOG_BASE_REQUIRED = ['name', 'owner', 'plugins'] as const;

/** The IS-overlay required delta (beyond the base floor). */
export const MARKETPLACE_CATALOG_OVERLAY_REQUIRED = [
  'version',
  'description',
  'license',
  'homepage',
  'keywords',
] as const;

/** Effective required = base ∪ overlay = the IS 8-field marketplace set (NON-NEGOTIABLE). */
export const MARKETPLACE_CATALOG_REQUIRED_FIELDS = [
  ...MARKETPLACE_CATALOG_BASE_REQUIRED,
  ...MARKETPLACE_CATALOG_OVERLAY_REQUIRED,
] as const;

// ─── Constraint constants (mirror the JSON Schemas exactly) ──────────────────

/** code.claude.com plugin-marketplaces name reserved-name blocklist (upstream-base — documented, not.enum). */
export const MARKETPLACE_NAME_RESERVED = [
  'claude-code-marketplace',
  'claude-code-plugins',
  'claude-plugins-official',
  'claude-plugins-community',
  'claude-community',
  'anthropic-marketplace',
  'anthropic-plugins',
  'agent-skills',
  'anthropic-agent-skills',
  'knowledge-work-plugins',
  'life-sciences',
  'claude-for-legal',
  'claude-for-financial-services',
  'financial-services-plugins',
] as const;
/** IS kebab-case name surface (is-overlay — DR-062 relocated structural encoding). */
export const MARKETPLACE_NAME_PATTERN = /^[a-z0-9]([a-z0-9-]*[a-z0-9])?$/;
/** IS name length ceiling (is-overlay — DR-062 relocated structural encoding). */
export const MARKETPLACE_NAME_MAX = 64;
/** Strict SemVer 2.0.0 (is-overlay — stricter than the legacy IS prefix match). */
export const SEMVER_PATTERN =
  /^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)(?:-((?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*)(?:\.(?:0|[1-9]\d*|\d*[a-zA-Z-][0-9a-zA-Z-]*))*))?(?:\+([0-9a-zA-Z-]+(?:\.[0-9a-zA-Z-]+)*))?$/;

function isStringArray(value: unknown): value is string[] {
  return Array.isArray(value) && value.every((item) => typeof item === 'string');
}

function isPlainObject(value: unknown): value is Record<string, unknown> {
  return typeof value === 'object' && value !== null && !Array.isArray(value);
}

function isUri(value: string): boolean {
  try {
    new URL(value);
    return true;
  } catch {
    return false;
  }
}

// ─── Layer 1: upstream base (authored by THEM) ───────────────────────────────

/**
 * The DR-062 projection-mirrored v2 base (REGENERATED from the captured
 * projection, spec_version unversioned-2026-06-12 — not a byte-copy of the
 * frozen v1 base). Required presence of upstream's standardFloor
 * ([name, owner, plugins]) + type/constraint on the FULL documented
 * surface: name as a bare string guarded by the documented 14-entry
 * reserved-name blocklist (the 061 #5 carve-out — kebab-case is prose-only
 * upstream, so the structural encoding lives in the overlay per DR-062 C3);
 * owner as an object with a required inner name; plugins as an array of
 * entry objects each requiring [name, source] with NO minItems (the doc
 * states no minimum); the documented top-level optionals $schema,
 * description, version, allowCrossMarketplaceDependenciesOn; and the
 * DOCUMENTED metadata surface (metadata.pluginRoot + the
 * description/version back-compat keys) — unlike the sibling contracts,
 * this contract's metadata is upstream-documented, so it lives in the
 * BASE. The 18 documented entry optionals + the 5 documented source forms
 * (./-prefixed relative-path string; github/url/git-subdir/npm object
 * types under the `source` discriminator) are enforced by the published
 * JSON Schema (ajv); this generated layer checks the entry floor + the
 * top-level surface only (depth boundary — the hook-config posture).
 */
export function upstreamBaseIssues(artifact: AuthoringArtifact): FoldIssue[] {
  const issues: FoldIssue[] = [
    ...requiredFieldsIssues(artifact, MARKETPLACE_CATALOG_BASE_REQUIRED),
  ];

  if ('$schema' in artifact && typeof artifact['$schema'] !== 'string') {
    issues.push({ message: '$schema must be a string', path: ['$schema'] });
  }

  if ('name' in artifact) {
    const name = artifact['name'];
    if (typeof name !== 'string') {
      issues.push({ message: 'name must be a string', path: ['name'] });
    } else if ((MARKETPLACE_NAME_RESERVED as readonly string[]).includes(name)) {
      issues.push({
        message: 'name must not be an upstream-reserved marketplace name',
        path: ['name'],
      });
    }
  }

  if ('owner' in artifact) {
    const owner = artifact['owner'];
    if (!isPlainObject(owner)) {
      issues.push({ message: 'owner must be an object', path: ['owner'] });
    } else {
      for (const key of ['name'] as const) {
        if (!(key in owner)) {
          issues.push({ message: `owner.${key} is required`, path: ['owner', key] });
        }
      }
    }
  }

  if ('plugins' in artifact) {
    const plugins = artifact['plugins'];
    if (!Array.isArray(plugins)) {
      issues.push({ message: 'plugins must be an array', path: ['plugins'] });
    } else {
      plugins.forEach((entry, index) => {
        if (!isPlainObject(entry)) {
          issues.push({
            message: 'plugins entry must be an object',
            path: ['plugins', String(index)],
          });
          return;
        }
        for (const key of ['name', 'source'] as const) {
          if (!(key in entry)) {
            issues.push({
              message: `plugins[${index}].${key} is required`,
              path: ['plugins', String(index), key],
            });
          }
        }
      });
    }
  }

  if ('description' in artifact && typeof artifact['description'] !== 'string') {
    issues.push({ message: 'description must be a string', path: ['description'] });
  }

  if ('version' in artifact && typeof artifact['version'] !== 'string') {
    issues.push({ message: 'version must be a string', path: ['version'] });
  }

  if (
    'allowCrossMarketplaceDependenciesOn' in artifact &&
    !Array.isArray(artifact['allowCrossMarketplaceDependenciesOn'])
  ) {
    issues.push({
      message: 'allowCrossMarketplaceDependenciesOn must be an array',
      path: ['allowCrossMarketplaceDependenciesOn'],
    });
  }

  if ('metadata' in artifact && !isPlainObject(artifact['metadata'])) {
    issues.push({ message: 'metadata must be an object', path: ['metadata'] });
  }

  return issues;
}

// ─── Layer 3: IS overlay (authored by US) ────────────────────────────────────

/**
 * The IS-only v2 delta: two upstream-optional fields promoted to
 * IS-required (version — NARROWED to strict SemVer 2.0.0 — and
 * description) + three net-new IS-required fields (license, homepage URI,
 * keywords) + the RELOCATED plugins minItems 1 (DR-062 C3, 061 #1 — the
 * corpus-rationalized minimum the doc does not state) + the RELOCATED
 * structural name encoding (kebab pattern + 64-char cap; DR-062 C3,
 * 061 #5 — prose-only upstream). NO metadata entry here: the documented
 * metadata surface lives in the v2 base (the 061 #2 convergence condition
 * is already satisfied upstream). A field whose type is already enforced
 * by the base is not re-typed here — its presence is the required check —
 * to keep messages single-sourced.
 */
export function isOverlayIssues(artifact: AuthoringArtifact): FoldIssue[] {
  const issues: FoldIssue[] = [
    ...requiredFieldsIssues(artifact, MARKETPLACE_CATALOG_OVERLAY_REQUIRED),
  ];

  if ('version' in artifact) {
    const version = artifact['version'];
    if (typeof version !== 'string') {
      issues.push({ message: 'version must be a string', path: ['version'] });
    } else if (!SEMVER_PATTERN.test(version)) {
      issues.push({ message: 'version must be strict SemVer 2.0.0', path: ['version'] });
    }
  }

  if ('license' in artifact && typeof artifact['license'] !== 'string') {
    issues.push({ message: 'license must be a string', path: ['license'] });
  }

  if ('homepage' in artifact) {
    const homepage = artifact['homepage'];
    if (typeof homepage !== 'string') {
      issues.push({ message: 'homepage must be a string', path: ['homepage'] });
    } else if (!isUri(homepage)) {
      issues.push({ message: 'homepage must be a valid URI', path: ['homepage'] });
    }
  }

  if ('keywords' in artifact && !isStringArray(artifact['keywords'])) {
    issues.push({ message: 'keywords must be an array of strings', path: ['keywords'] });
  }

  if ('name' in artifact) {
    const name = artifact['name'];
    if (typeof name !== 'string') {
      issues.push({ message: 'name must be a string', path: ['name'] });
    } else {
      if (name.length > MARKETPLACE_NAME_MAX) {
        issues.push({
          message: `name must be at most ${MARKETPLACE_NAME_MAX} characters`,
          path: ['name'],
        });
      }
      if (!MARKETPLACE_NAME_PATTERN.test(name)) {
        issues.push({
          message: 'name must be kebab-case (lowercase letters, digits, hyphens)',
          path: ['name'],
        });
      }
    }
  }

  if ('plugins' in artifact) {
    const plugins = artifact['plugins'];
    if (!Array.isArray(plugins)) {
      issues.push({ message: 'plugins must be an array', path: ['plugins'] });
    } else if (plugins.length < 1) {
      issues.push({ message: 'plugins must not be empty', path: ['plugins'] });
    }
  }

  return issues;
}

// ─── The composition (allOf of base + universal folds + overlay) ─────────────

/** Every issue from the three composed layers, in layer order. */
export function marketplaceCatalogIssues(artifact: AuthoringArtifact): FoldIssue[] {
  return [
    ...upstreamBaseIssues(artifact),
    ...universalFoldsIssues(artifact),
    ...isOverlayIssues(artifact),
  ];
}

/** The marketplace-catalog contract — pure allOf composition (base ∧ universalFolds ∧ overlay). */
export const MarketplaceCatalogSchema = attach(marketplaceCatalogIssues);

export type MarketplaceCatalog = AuthoringArtifact;
