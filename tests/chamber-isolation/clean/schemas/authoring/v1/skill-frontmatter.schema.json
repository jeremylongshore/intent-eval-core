{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/skill-frontmatter.schema.json",
  "$comment": "A clean authoring schema. Its trust anchor stays inside the authoring chamber and never resolves to a runtime predicate / signing trust root.",
  "type": "object",
  "required": ["name", "description"]
}
