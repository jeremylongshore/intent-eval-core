{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/hook-config.schema.json",
  "title": "hook-config — IS marketplace-tier contract (composition)",
  "description": "The published, canonical hook-config authoring contract (a single flattened hooks.json handler entry). PURE COMPOSITION — zero authored fields of its own. It is the allOf of three layers: (1) the upstream base (code.claude.com/docs/en/hooks + code.claude.com/docs/en/settings, authored by THEM); (2) the three universal folds (deprecationRegistry, securityChecks, disclosureMarkers) by $ref to the shared foundation; (3) the IS overlay (authored by US). This is authoring contract #5 — codegen-generated (DR-044 D8). The Zod validator AND this inline $comment effective-required manifest are GENERATED by scripts/codegen-authoring.ts from the base+overlay JSON Schemas — never hand-typed.",
  "$comment": "EFFECTIVE-REQUIRED MANIFEST (the answer to 'what does hook-config require?' from one file — DR-044 D7(e)). This block is the generated effective-required surface; the source of truth is the `required` arrays of the two composed layers below.\n  event        INHERITED   (upstream-base · code.claude.com hooks standardFloor)\n  matcher      INHERITED   (upstream-base · code.claude.com hooks standardFloor)\n  type         INHERITED   (upstream-base · code.claude.com hooks standardFloor)\n  command      INHERITED   (upstream-base · code.claude.com hooks standardFloor)\n  description  REQUIRED HERE (is-overlay · net-new IS tracking field)\n  enabled      REQUIRED HERE (is-overlay · net-new IS tracking field)\n  timeout      REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  blocking     REQUIRED HERE (is-overlay · net-new IS tracking field)\nEffective required = base ∪ overlay. Citations: code.claude.com/docs/en/hooks (Hook lifecycle table + handler fields); code.claude.com/docs/en/settings; tests/authoring/v1/fixtures/hook-config/ (corpus-first ground truth).",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/upstream-base/hook-config.v1.json"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/marketplace-tier.schema.json#/$defs/universalFolds"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/is-overlay/hook-config.v1.json"
    }
  ]
}
