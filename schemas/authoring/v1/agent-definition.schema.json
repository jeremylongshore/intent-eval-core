{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/agent-definition.schema.json",
  "title": "agent-definition — IS marketplace-tier contract (composition)",
  "description": "The published, canonical agent-definition authoring contract (the agents/*.md YAML frontmatter shape). PURE COMPOSITION — zero authored fields of its own. It is the allOf of three layers: (1) the upstream base (code.claude.com/docs/en/sub-agents, authored by THEM); (2) the three universal folds (deprecationRegistry, securityChecks, disclosureMarkers) by $ref to the shared foundation; (3) the IS overlay (authored by US). This is authoring contract #3 — codegen-generated (DR-044 D8). The Zod validator AND this inline $comment effective-required manifest are GENERATED by scripts/codegen-authoring.ts from the base+overlay JSON Schemas — never hand-typed.",
  "$comment": "EFFECTIVE-REQUIRED MANIFEST (the answer to 'what does agent-definition require?' from one file — DR-044 D7(e)). This block is the generated effective-required surface; the source of truth is the `required` arrays of the two composed layers below.\n  name         INHERITED   (upstream-base · code.claude.com sub-agents standardFloor)\n  description  INHERITED   (upstream-base · code.claude.com sub-agents standardFloor)\n  tools        REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  model        REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  color        REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  version      REQUIRED HERE (is-overlay · net-new IS tracking field)\n  author       REQUIRED HERE (is-overlay · net-new IS tracking field)\n  tags         REQUIRED HERE (is-overlay · pure IS invention, zero upstream provenance)\nEffective required = base ∪ overlay. Citations: code.claude.com/docs/en/sub-agents § 'Supported frontmatter fields'; tests/authoring/v1/fixtures/agent-definition/ (corpus-first ground truth).",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/upstream-base/agent-definition.v1.json"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/marketplace-tier.schema.json#/$defs/universalFolds"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v1/is-overlay/agent-definition.v1.json"
    }
  ]
}
