{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/marketplace-catalog.schema.json",
  "title": "marketplace-catalog — IS marketplace-tier v2 contract (projection-mirrored composition)",
  "description": "The v2 marketplace-catalog authoring contract (the `.claude-plugin/marketplace.json` catalog shape). PURE COMPOSITION — zero authored fields of its own. It is the allOf of three v2 layers: (1) the upstream base, REGENERATED from the captured projection per DR-062 (documented fields only, upstream requiredness — no minItems on plugins, name a bare string guarded by the documented 14-name reserved blocklist — and upstream wire forms incl. the 18 documented entry optionals + the 5 documented source forms); (2) the three v2 universal folds by $ref to the v2 marketplace-tier foundation; (3) the v2 IS overlay (the five carried IS promotions + the relocated plugins minItems 1 + the relocated structural name encoding). v2 has ZERO $ref into the frozen v1 family (copy-then-regenerate); the v1 marketplace-catalog composition stays the PUBLISHED contract. The Zod validator AND this inline $comment effective-required manifest are GENERATED by scripts/codegen-authoring.ts from the base+overlay JSON Schemas — never hand-typed. Lifecycle SHIPPED-INTERNAL. Adjudication: DR-062 (marketplace-catalog rows C1 = 061 #2/#3/#4 + the #5 reserved-names carve-out, C3 = 061 #1/#5, C4 = 061 #6 not adopted).",
  "$comment": "EFFECTIVE-REQUIRED MANIFEST (the answer to 'what does marketplace-catalog require?' from one file — DR-044 D7(e)). This block is the generated effective-required surface; the source of truth is the `required` arrays of the two composed layers below.\n  name         INHERITED   (upstream-base · code.claude.com plugin-marketplaces standardFloor)\n  owner        INHERITED   (upstream-base · code.claude.com plugin-marketplaces standardFloor)\n  plugins      INHERITED   (upstream-base · code.claude.com plugin-marketplaces standardFloor)\n  version      REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  description  REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  license      REQUIRED HERE (is-overlay · net-new IS tracking field)\n  homepage     REQUIRED HERE (is-overlay · net-new IS tracking field)\n  keywords     REQUIRED HERE (is-overlay · net-new IS tracking field)\nEffective required = base ∪ overlay = the IS 8-field marketplace-catalog set (preserved from the v1 composition VERBATIM — the DR-062 relocation changes WHERE the narrowings live, not what the composed IS marketplace tier requires). The base ADDITIONALLY carries the per-entry floor ([name, source] on every plugins item) and the per-source-form conditional requiredness (github ⇒ repo, url ⇒ url, git-subdir ⇒ path+url, npm ⇒ package) — nested/conditional, so not manifest rows. Citations: DR-062; specs/_vendor/upstream/marketplace-catalog/projection.json (sha256 01d24b5332c29397636fe34265e82e232c5f48722706fbaadec1846dec664092); code.claude.com/docs/en/plugin-marketplaces.",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/upstream-base/marketplace-catalog.v2.json"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/marketplace-tier.schema.json#/$defs/universalFolds"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/is-overlay/marketplace-catalog.v2.json"
    }
  ]
}
