{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/hook-config.schema.json",
  "title": "hook-config — IS marketplace-tier v2 contract (projection-mirrored composition)",
  "description": "The v2 hook-config authoring contract (a hooks.json / settings.json hooks-block document). PURE COMPOSITION — zero authored fields of its own. It is the allOf of three v2 layers: (1) the upstream base, REGENERATED from the captured projection per DR-062 (the documented 3-level nesting replaces v1's flattened single-handler shape, the 18 documented handler fields, per-type conditional requiredness for all five handler types, matcher match-all forms accepted); (2) the three v2 universal folds by $ref to the v2 marketplace-tier foundation; (3) the v2 IS overlay (the relocated explicit-non-empty-matcher requirement + the carried per-handler IS quartet + the relocated `metadata` extension). v2 has ZERO $ref into the frozen v1 family (copy-then-regenerate); the v1 hook-config composition stays the flattened PUBLISHED contract. The Zod validator AND this inline $comment effective-required manifest are GENERATED by scripts/codegen-authoring.ts from the base+overlay JSON Schemas — never hand-typed. Lifecycle SHIPPED-INTERNAL. Adjudication: DR-062 (hook-config rows C1 = 060 #1/#3/#5, C2 = 060 #2, C3 = 060 #4, C4 = 060 #6 NOT adopted).",
  "$comment": "EFFECTIVE-REQUIRED MANIFEST (the answer to 'what does hook-config require?' from one file — DR-044 D7(e)). This block is the generated effective-required surface; the source of truth is the `required` arrays of the two composed layers below.\n  hooks  INHERITED   (upstream-base · code.claude.com hooks standardFloor)\nEffective required = base ∪ overlay = [hooks] at the TOP LEVEL only — this contract's IS requiredness binds INSIDE the nesting (per-group [matcher]; per-handler [type ∪ the per-type conditional ∪ description, enabled, timeout, blocking]) via the base's nested shapes + the overlay's deep narrowings, reproducing the v1 composed 8-field per-handler surface under the adopted 3-level nesting. Conditional/deep requiredness is not a manifest row (same rule as mcp-config's per-transport conditional). Citations: DR-062; specs/_vendor/upstream/hook-config/projection.json (sha256 58b4c144f08aaaf524050f804d7e6d71939d7c7d8128ab1a307b4e6094cfde09); code.claude.com/docs/en/hooks; code.claude.com/docs/en/settings.",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/upstream-base/hook-config.v2.json"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/marketplace-tier.schema.json#/$defs/universalFolds"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/is-overlay/hook-config.v2.json"
    }
  ]
}
