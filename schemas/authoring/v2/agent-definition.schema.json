{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/agent-definition.schema.json",
  "title": "agent-definition — IS marketplace-tier v2 contract (projection-mirrored composition)",
  "description": "The v2 agent-definition authoring contract (the YAML frontmatter shape of a subagent file, agents/*.md). PURE COMPOSITION — zero authored fields of its own. It is the allOf of three v2 layers: (1) the upstream base, REGENERATED from the captured projection per DR-062 (documented fields only — incl. the 11 fields v1 never modeled — upstream requiredness [name, description], upstream wire forms: tools string|array, model full-ID latitude, name bare string); (2) the three v2 universal folds by $ref to the v2 marketplace-tier foundation; (3) the v2 IS overlay (the relocated tools/model narrowings + the carried IS promotions and tracking trio + the relocated structural name encoding + the relocated `metadata` extension). v2 has ZERO $ref into the frozen v1 family (copy-then-regenerate); the v1 agent-definition composition stays the narrower-base PUBLISHED contract. The Zod validator AND this inline $comment effective-required manifest are GENERATED by scripts/codegen-authoring.ts from the base+overlay JSON Schemas — never hand-typed. Lifecycle SHIPPED-INTERNAL. Adjudication: DR-062 (agent-definition rows: C1 = 059 #1, C2 = 059 #3/#4, C3 = 059 #2/#5, C4 = 059 #6 NOT adopted).",
  "$comment": "EFFECTIVE-REQUIRED MANIFEST (the answer to 'what does agent-definition require?' from one file — DR-044 D7(e)). This block is the generated effective-required surface; the source of truth is the `required` arrays of the two composed layers below.\n  name         INHERITED   (upstream-base · code.claude.com sub-agents standardFloor)\n  description  INHERITED   (upstream-base · code.claude.com sub-agents standardFloor)\n  tools        REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  model        REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  color        REQUIRED HERE (is-overlay · promoted from upstream-optional)\n  version      REQUIRED HERE (is-overlay · net-new IS tracking field)\n  author       REQUIRED HERE (is-overlay · net-new IS tracking field)\n  tags         REQUIRED HERE (is-overlay · pure IS invention, zero upstream provenance)\nEffective required = base ∪ overlay = the IS 8-field agent-definition set (preserved from the v1 composition VERBATIM — the DR-062 relocation changes WHERE the narrowings live, not what the composed IS marketplace tier requires). Citations: DR-062; specs/_vendor/upstream/agent-definition/projection.json (sha256 7ff25f146d3462ee1d99ec6c0c482df899fad1d617552cb09e3594034e852a56); code.claude.com/docs/en/sub-agents.",
  "type": "object",
  "allOf": [
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/upstream-base/agent-definition.v2.json"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/marketplace-tier.schema.json#/$defs/universalFolds"
    },
    {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/authoring/v2/is-overlay/agent-definition.v2.json"
    }
  ]
}
