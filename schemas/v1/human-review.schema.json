{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/human-review.schema.json",
  "title": "HumanReview — open-ended human-trust signal on an EvalRun (net-new canonical entity, ISEDC DR-103 D1)",
  "description": "A single human's open-ended assessment of an EvalRun — the human counterpart to JudgeDecision (whose machine verdict is the `eval-verdict/v1` predicate, NOT `judge-decision/v1`). Three ORTHOGONAL nullable channels capture the Langfuse-style signal set without crushing them into a scalar: `score_text` (open-ended NON-COMPARABLE TEXT, intentionally not an enum/number — ISEDC DR-103 C3 B6.3), `thumbs` (boolean), `annotation` (free text). Append-only ledger; single terminal state `recorded`; a revision is a NEW row citing `supersedes_id`, never a mutation. ANTI-GAMING (ISEDC DR-103 D1 B1.2, machine-enforced): a row MUST pin to a verified source — `session_trace_id` non-null OR `judge_decision_id` non-null (a lone thumb pinned to nothing is refused), AND a service-account-authored row is refused (`reviewer_is_service_account` MUST be false — the `human_reviews` ledger is a HUMAN-only signal), AND at least one of the three signal channels MUST be present. ADDITIVE / one-way-door per ISEDC DR-103 D1: this shape ships in signed @intentsolutions/core entries, so widening or merging it post-publish is irreversible — additive only. `tenant_id` is OPTIONAL-NOT-NULLABLE (DR-085 D5 / deferral-G — byte-identical to EvalSpec/EvalRun/SkillSnapshot/SkillVersion). The non-reproducible human-trust signal does NOT ride the GateResultV1-pinned EvidenceStatement; it gets its own parallel additive HumanReviewStatement (binds subject[0].digest.sha256 === input_hash only) and the `human-review/v1` predicate, which is PERMANENTLY sigstore_staging by design (ISEDC DR-103 D3 B3.2 — a human's open-ended TEXT assessment can never meet rekor_production's reproduce-the-hash bar).",
  "$comment": "ISEDC DR-103 D1 B1.2 + B1.3 + D3 B3.2 (intent-eval-lab 103-AT-DECR-isedc-skill-scoring-kernel-contracts-2026-06-25.md). The anti-gaming cross-field invariant (`session_trace_id` non-null OR `judge_decision_id` non-null; `reviewer_is_service_account` === false; at-least-one signal channel non-null) is machine-enforced at all three layers: this JSON-Schema `allOf`/`anyOf`/`const`, the Zod `.superRefine` in src/validators/v1/human-review.ts, and the Pydantic `model_validator` wrapper in python/.../models.py. This entity is its OWN triplet (separate additive minor) — it is NOT co-shipped with usage_events (ISEDC DR-103 D1 B1.1 two-triplets binding).",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "eval_run_id",
    "session_trace_id",
    "judge_decision_id",
    "supersedes_id",
    "reviewer_identity",
    "reviewer_is_service_account",
    "score_text",
    "thumbs",
    "annotation",
    "input_hash",
    "created_at"
  ],
  "properties": {
    "id": {
      "$ref": "_common.schema.json#/$defs/uuidv7",
      "description": "UUIDv7 PK. The `human_review_id` the human-review/v1 predicate body references."
    },
    "eval_run_id": {
      "$ref": "_common.schema.json#/$defs/uuidv7",
      "description": "FK -> EvalRun.id (required). The EvalRun this human is assessing."
    },
    "session_trace_id": {
      "oneOf": [
        {
          "$ref": "_common.schema.json#/$defs/uuidv7"
        },
        {
          "type": "null"
        }
      ],
      "description": "FK -> SessionTrace.id, NULLABLE. ANTI-GAMING (DR-103 D1 B1.2): a review MUST pin to a verified source — this OR `judge_decision_id` MUST be non-null. A review pinned to nothing is forgery-cost-zero and is refused at all three layers."
    },
    "judge_decision_id": {
      "oneOf": [
        {
          "$ref": "_common.schema.json#/$defs/uuidv7"
        },
        {
          "type": "null"
        }
      ],
      "description": "FK -> JudgeDecision.id, NULLABLE. The machine verdict the human is agreeing-with / overriding (JudgeDecision's signed predicate is `eval-verdict/v1`). ANTI-GAMING (DR-103 D1 B1.2): this OR `session_trace_id` MUST be non-null (pin to something verified)."
    },
    "supersedes_id": {
      "oneOf": [
        {
          "$ref": "_common.schema.json#/$defs/uuidv7"
        },
        {
          "type": "null"
        }
      ],
      "description": "Convenience pointer to the prior HumanReview this row revises (HumanReview -> HumanReview). `null` for the first review. No record is ever mutated — a revision is a NEW row citing the prior (append-only ledger)."
    },
    "reviewer_identity": {
      "$ref": "_common.schema.json#/$defs/actorIdentity",
      "description": "Identity of the human reviewer (engineer email, account name). The human-trust signal's authorship anchor. DR-103 D3 B3.2 names verified reviewer identity + the pinned session as `human-review/v1`'s REAL trust criterion (the predicate being permanently staging is by design, NOT a half-finished state)."
    },
    "reviewer_is_service_account": {
      "type": "boolean",
      "const": false,
      "description": "ANTI-GAMING (DR-103 D1 B1.2): the `human_reviews` ledger is a HUMAN-only signal, so a service-account-authored row is EXCLUDED structurally — this MUST be `false`. A service-account assessment is not a human-trust signal and cannot be laundered into the human-signal counts. Carried as a `const false` field (not omitted) so the exclusion is an explicit, audited claim on every row rather than an implicit assumption."
    },
    "score_text": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "description": "Open-ended human assessment as NON-COMPARABLE free TEXT — intentionally NOT an enum or number (DR-103 C3 B6.3). Consumers MUST NOT parse this into a scalar or aggregate it; a numeric channel that needs comparison is a SEPARATE structured field, never this string. Nullable: a review may carry only a thumb and/or an annotation."
    },
    "thumbs": {
      "oneOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "description": "Coarse up/down signal. `true` = thumbs-up, `false` = thumbs-down, `null` = no thumb on this review. One of the three ORTHOGONAL channels (DR-103 C3 B6.3) — never averaged with the others."
    },
    "annotation": {
      "oneOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "description": "Free-text annotation / comment from the reviewer. Distinct from `score_text` (which is the open-ended SCORE channel) — an annotation is contextual prose, a score_text is the human's open-ended assessment. Nullable. One of the three ORTHOGONAL channels (DR-103 C3 B6.3)."
    },
    "input_hash": {
      "$ref": "_common.schema.json#/$defs/sha256",
      "description": "BARE 64-hex sha256 of the canonical input the human reviewed. The parallel HumanReviewStatement binds `subject[0].digest.sha256 === input_hash` (DR-103 D1 B1.3) — that pin IS the forgery cost. BARE alphabet (aligned to the entity-hash convention, e.g. SkillSnapshot.combined_sha); the in-toto wire layer strips/adds the `sha256:` prefix at the boundary."
    },
    "created_at": {
      "$ref": "_common.schema.json#/$defs/rfc3339"
    },
    "tenant_id": {
      "$ref": "_common.schema.json#/$defs/uuidv7",
      "description": "RESERVED multi-tenancy slot (deferral-G, bd_000-projects-k0fj; DR-085 D5), OPTIONAL-NOT-NULLABLE and byte-identical to EvalSpec/EvalRun/SkillSnapshot/SkillVersion. An absent tenant is a first-class single-tenant/global state, NEVER pooled into a cross-tenant aggregate (DR-103 D2 B2.2). v1 single-tenant reviews omit it; tenant-isolation semantics deferred to a future DR."
    }
  },
  "allOf": [
    {
      "$comment": "ANTI-GAMING (DR-103 D1 B1.2): a review MUST pin to a verified source. Expressed as if/then so codegen emits a clean structural model (it ignores if/then) while AJV enforces it: IF both session_trace_id AND judge_decision_id are null, THEN reject (then:false). A row pinned to nothing is forgery-cost-zero. Also machine-enforced in the Zod .superRefine + the Pydantic model_validator.",
      "if": {
        "properties": {
          "session_trace_id": {
            "type": "null"
          },
          "judge_decision_id": {
            "type": "null"
          }
        },
        "required": ["session_trace_id", "judge_decision_id"]
      },
      "then": false
    },
    {
      "$comment": "AT-LEAST-ONE-SIGNAL (spec Item 2): at least one of the three orthogonal channels (score_text, thumbs, annotation) MUST be non-null. Expressed as if/then so codegen emits a clean structural model: IF all three are null, THEN reject. An empty review carries no human signal. Also machine-enforced in Zod + Pydantic.",
      "if": {
        "properties": {
          "score_text": {
            "type": "null"
          },
          "thumbs": {
            "type": "null"
          },
          "annotation": {
            "type": "null"
          }
        },
        "required": ["score_text", "thumbs", "annotation"]
      },
      "then": false
    }
  ]
}
