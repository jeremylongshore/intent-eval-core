{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://evals.intentsolutions.io/dashboard-render/v1.schema.json",
  "title": "dashboard-render/v1 — in-toto predicate body attesting a rendered dashboard artifact",
  "description": "Predicate body of an in-toto Statement v1 whose predicateType is https://evals.intentsolutions.io/dashboard-render/v1. Attests that a specific rendered dashboard HTML artifact was produced from a specific, content-addressed set of evidence inputs — i.e., 'this rendered page is a faithful function of exactly these signed bundles.' Enables sign-your-own-homework verification: a third party can re-run the renderer against the same inputs and reproduce rendered_artifact.content_hash. This schema validates ONLY the predicate body — the enclosing in-toto Statement envelope (_type, subject, predicateType) is validated separately. Each row independently verifiable; NO top-level bundle signature per Blueprint B § 7 line 754.\n\nADDITIVE in kernel v0.2.0 (B3 binding, sequenced): net-new predicate URI; no v0.1 contract changes. Per § 7.2 backward-compat, adding a predicate URI is allowed. Runs in sigstore_staging until a second independent verifier exists (sign-your-own-homework sequencing) and production-Rekor unlock per DR-010 Q3.",
  "type": "object",
  "additionalProperties": false,
  "required": ["rendered_artifact", "input_bundles", "rendered_at", "renderer"],
  "properties": {
    "rendered_artifact": {
      "type": "object",
      "description": "The rendered dashboard artifact this row attests.",
      "additionalProperties": false,
      "required": ["content_hash"],
      "properties": {
        "uri": {
          "type": "string",
          "description": "OPTIONAL location of the rendered artifact — relative path or absolute URL. The content_hash is the authoritative identifier; uri is a convenience pointer.",
          "minLength": 1
        },
        "content_hash": {
          "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/_common.schema.json#/$defs/sha256Prefixed",
          "description": "sha256-prefixed digest of the rendered HTML artifact bytes. Reproducing this from input_bundles is what a verifier checks."
        },
        "media_type": {
          "type": "string",
          "description": "OPTIONAL IANA media type of the rendered artifact (e.g., text/html).",
          "minLength": 1
        }
      }
    },
    "input_bundles": {
      "type": "array",
      "description": "Content-addressed evidence inputs the artifact was rendered from. Each entry pins a bundle by id and/or digest so the input set is reproducible. MUST be non-empty — a render with no evidence inputs is not attestable.",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "minProperties": 1,
        "properties": {
          "bundle_id": {
            "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/_common.schema.json#/$defs/uuidv7",
            "description": "UUIDv7 of an input EvidenceBundle (FK → EvidenceBundle.id)."
          },
          "content_hash": {
            "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/_common.schema.json#/$defs/sha256Prefixed",
            "description": "sha256-prefixed digest of the input bundle payload, pinning exact content."
          }
        }
      }
    },
    "rendered_at": {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/_common.schema.json#/$defs/rfc3339",
      "description": "RFC 3339 UTC timestamp at which the render was produced."
    },
    "renderer": {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/_common.schema.json#/$defs/runnerIdentifier",
      "description": "Renderer tool identity + version in the form `<kebab-slug>@<semver>` (e.g., intent-eval-dashboard@0.2.0). Mirrors gate-result/v1's `runner` discipline so the producing tool is always pinned."
    },
    "renderer_config_hash": {
      "$ref": "https://github.com/jeremylongshore/intent-eval-core/schemas/v1/_common.schema.json#/$defs/sha256Prefixed",
      "description": "OPTIONAL sha256-prefixed hash of the renderer configuration / template set, so a verifier can pin not just the tool version but the exact template inputs."
    }
  }
}
